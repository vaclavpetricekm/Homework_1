[project]
name = "3-function-calls"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-weather>=2.0.0",
    "openai>=1.66.3",
    "python-dotenv>=1.0.1",
]
